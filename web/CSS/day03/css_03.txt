1.边框尺寸
	1.轮廓线
		outline
		默认情况下文本框密码框都带轮廓线
		outline:none;  #取消轮廓线
	2.box-sizing
		改变元素尺寸的计算方式
		1.默认取值:content-box
			元素实际尺寸由 margin+border+padding+width/height 计算得到
		2.取值border-box
			元素实际尺寸:width=margin+border+padding+内容
			一旦为元素设置边距和边框,会压缩内容区域
2.背景属性
	1.背景颜色
		属性 background-color
		取值 颜色值
	2.背景图片
		属性 background-image
		取值 url(路径)
	3.背景图片相关属性
		1.背景图片重复平铺显示
			属性 background-repeat
			取值 
				1.repeat 默认值,图片尺寸不够时,沿水平和垂直方向重复平铺
				2.repeat-x 沿水平方向重复平铺
				3.repeat-y 沿垂直方向重复平铺
				3.no-repeat 不重复平铺
		2.背景图片尺寸
			属性 background-size
			取值(取两个值,作为背景图片的宽高)
				1.px 分别设置背景图片的宽高
				2.% 采用当前元素的尺寸占比作为背景图片的尺寸
				====以下作为了解部分====
				3.cover 表示对图片等比缩放至完全覆盖元素,多出部分裁剪掉
				4.contain 对图片等比缩放至刚好容纳图片,不足的部分不管
				5.auto 自动,按照原始尺寸平铺
		3.背景图片位置
			属性 background-position
			取值 
				1.px 设置水平和垂直方向的偏移距离
				2.x% y%
				3.方位值
					top center bottom
					left center right
					设置背景图片水平和垂直方向上的显示位置,某一个方向缺省的话,默认为center 
		4.背景图片是否固定
			属性 background-attachment
			设置背景图片是否跟随网页滚动
			取值
				1.scroll 默认值,跟随网页滚动
				2.fixed 固定
					1.给任何元素添加背景图片固定,当前背景图片是相对于视口viewport 和当前容器元素几乎没有关系
					2.当容器元素不可见时,背景图片也不可见
	4.背景属性简写
		属性 background
		取值 color url position repeat size attachment
		e.g.
			background : red url('') right no-repeat scroll;
		*background-size 是CSS3属性,需要单独设置

3.文本/字体属性
	1.指定字体
		font-family:"Microsoft YaHei","黑体",Arial(宋体);
		注意:当字体名称为中文,或者是由多个英文单词组成的,都需要添加引号
	2.字体粗细
		font-weight
		取值:
			1.lighter(100)/normal(400)/bold(700)
			2.采用数字,无单位,取值100-900,值越大,字体越粗
	3.字体大小
		font-size
		取值:16px
	4.字体样式(斜体)
		font-style
		取值
			1.normal
			2.italic 斜体
			3.oblique 字体倾斜 显示效果与斜体一致
	5.行高
		line-height 常用于设置文本垂直居中
	6.字体属性简写
		使用简写有必填属性值 family
		font:style weight size family;
		font:family size/line-height;
		e.g.
			font:Arial 16px/20px;
		注意:
			1.font-weight font-style必须放在font-size之前
			2.同时设置字体大小和行高,必须写成一个属性值存在,size/line-height
	7.文本属性
		1.文本颜色 color
		2.文本装饰线 text-decoration
			取值:1.underline 下划线 2.none 取消 3.overline 上划线 4.line-through 删除线
		3.首行缩进 text-indent
			取值: 像素值

4.表格属性
	1.添加边框
		border : 1px solid black;
	2.表格边框合并
		属性 border-collapse
		取值
			1.separate(默认,边框分离)
			2.collapse(边框合并)
	3.边框边距
		设置单元格之间的距离
		属性:border-spacing
		取值
			1.指定一个数值
				水平与垂直方向上的间距相等
			2.指定两个数值
				分别设置水平方向与垂直方向上单元格之间的距离
		注意:必须在边框分离的情况下使用

5.列表属性
	1.列表的特征
		1.无序列表,有序列表都存在上下外边距
		1.无序列表,有序列表都存在左边内边距
		3.具备项目符号
		4.列表项是垂直排列的
	2.列表属性
		属性
			1.list-style-type 设置项目符号
			2.list-style-position 设置项目符号的位置 默认outside 项目符号与内容框分离,设置inside可以将项目符号调整到内容框中.不常用
			3.list-style-image :url() 自定义项目符号
		属性简写:
			list-style:type image position; 
			常用:
				list-style:none; #取消项目符号

6.过渡效果
	1.过渡效果指的是CSS属性值在一段时间内平缓变换的效果
	2.语法
		1.指定过渡属性
			指定哪个属性值在变化的时候使用过渡效果
			transition-property
			取值:
				1.all但凡能够使用过渡效果的属性值在变化时一律采用过渡来体现
				2.单独设置属性,指定某一种属性在变化时使用过渡效果
					允许使用过渡效果的属性:
						1.颜色相关的属性
						2.取值为数字的属性
		2.指定过渡时长
			transition-duration
			指定在多长时间内完成过渡效果
			取值:以 s 或者 ms 为单位的数值
				1s = 1000ms
				500ms = 0.5s/.5s
		3.指定过渡的速度时间曲线函数
			transition-timing-function
			取值:
				1.ease 默认值 慢速开始,快速变快,缓慢结束
				2.linear 匀速变换
				3.ease-in 慢速开始,加速结束
				4.ease-out 快速开始,慢速结束
				5.ease-in-out 慢速开始和结束,中间先加速后减速的过程
		4.指定过渡效果的延迟时间
			等激发过渡效果之后,等待多久开始执行
			transition-delay
			取值:以 s 或者 ms 为单位的数值
		5.属性简写
			transition:property duration timing-fun delay;

7.布局方式(重点)
	1.布局方式影响页面整体结构的划分和元素的显示样式
	2.布局方式分类
		1.文档流布局/普通流/静态流
			默认的布局方式
			元素按照书写顺序及元素类型,从上到下,从左到右排列
		2.浮动布局(重难点)
			最常用的布局方式
			1.将元素设置浮动之后,元素会具备以下特点
				1.元素会脱离文档流,不再占据文档空间
				2.剩余未浮动元素会上前占位
				3.元素浮动时,会停靠在父元素的左边或右边,或者是其他的浮动元素边缘上
				4.元素浮动只能在当前行浮动
				5.浮动可解决空行问题,也可以解决行内块元素水平间隙的问题
			2.语法
				属性 float
				取值
					1.left 元素向左浮动,碰到其他元素的边框时停止
					2.right 元素向右浮动,碰到其他元素的边框时停止
					3.none 默认值 元素不浮动
			3.浮动引发的效果
				1.如果父元素中无法容纳浮动元素,浮动元素会自动换行显示
				2.元素一旦设置浮动,便可以设置宽高,主要针对行内元素
				3.元素一旦浮动,默认尺寸由内容决定
				4.文字环绕效果
					如果前一个元素设置了浮动,后一个元素会上前占位,并且后面元素的文本会围绕浮动元素显示
			4.浮动元素带来的影响及解决
				1.由于浮动元素脱离文档流,在文档中不占位,所以在父元素中也相当于不存在.而父元素的高度是以为浮动元素的高度为准的,所以一旦所有子元素浮动,会造成父元素高度塌陷.
				2.解决方式
					1.针对父元素高度塌陷可以采用
						1.直接给父元素设置高度
							弊端:不一定每一次都清楚父元素的高度
						2.给父元素设置浮动(最不推荐)
							弊端:影响后面其他元素的布局
						3.直接给父元素添加一个属性overflow为hidden或auto
							弊端:如果出现内容溢出,会被隐藏不显示
						4.清除浮动带来的影响(标准方式)
							1.属性 clear 在正常元素中使用
							2.解决父元素高度塌陷,以及浮动元素遮挡正常元素的问题
							3.取值
								1.none 默认,不做任何操作
								2.left 清除当前元素前面元素左浮动带来的影响,不会被前面左浮动的元素压在底下
								3.right 清除当前元素前面元素右浮动带来的影响,不会被前面右浮动的元素压在底下
								4.both 清除当前元素前面元素任何一种浮动方式带来的影响
							4.解决父元素高度塌陷
								在父元素中添加空的子元素标签,设置子元素清除浮动
									clear:both;
								要求必须使用块级元素做子元素
		3.定位布局
			1.属性 position
			2.取值
				1.relative 相对定位
				2.absolute 绝对定位
				3.fixed 固定定位
				4.static 静态布局(默认值)
			3.偏移属性
				作用:配合已定位元素实现位置移动
				元素设置position属性为relative/absolute/fixed都被称为已定位元素
				属性 top/right/bottom/left
				取值 像素值
					top 以元素上边为基准进行偏移
					right 以元素右边为基准进行偏移
					bottom 以元素下边为基准进行偏移
					left 以元素左边为基准进行偏移
			4.相对定位
				1.元素会以相对于它在文档中的原来位置进行偏移
				2.元素使用相对定位进行偏移,在文档中始终占据原本的位置,一旦偏移,会遮挡其他元素
			5.绝对定位(重点,常用)
				1.特点
					1.元素使用绝对定位,会脱离文档流,在文档中不再占位
					2.绝对定位的元素会参照据它最近的一个已经定位的祖先元素进行漂移
					3.若无已经定位的祖先元素,元素将会参照body进行定位
				2.语法
					属性 position
					取值 absolute
					配合偏移属性实现定位
				3.注意
					1.由于绝对定位元素会脱离文档流,所以在使用绝对定位时,一般按照父元素相对定位,子元素绝对定位的原则进行布局(父相子绝)
					2.元素脱离文档流之后,都可以设置宽高(针对行内元素)
			6.固定定位
				1.元素一旦设置固定定位,会被固定在浏览器窗口的某个位置,不会跟随网页滚动而发生位置的改变
				2.语法
					属性 position
					取值 fixed
					配合偏移属性进行定位布局
				3.注意事项
					1.固定定位的元素永远相对于body实现位置初始化
					2.元素设置固定定位,就会变成块级元素
			7.元素的堆叠次序
				1.已定位的元素们堆叠在一起的排列顺序
				2.语法
					属性 z-index
					取值 数字,无单位,值越大越靠上
				3.注意
					1.只有已经定位的元素position:relative/absolute/fixed才可以使用z-index,否则不起作用.
					2.父子元素之间,无论如何修改z-index,子元素永远在上,父元素在下
					3.如果z-index取值相同,后来者居上
		4.元素隐藏与显示
			1.元素的显示方式
				1.元素以什么形式显示在页面中(行内/行内块/块)
				2.语法
					display:none/line/block/inline-block
					取值
						1.none 元素隐藏不显示,在文档中不占位
						2.inline 显示为行内元素
						3.block 显示为块元素
						4.inline-block 显示为行内块元素
							行内块元素兼具行内元素与块元素的特征,既可以共行,也可以设置宽高 img input
			2.元素显示效果
				1.可见性设置
					属性 visibility
					取值
						1.visible 默认值,可见
						2.hidden 隐藏,但元素在文档中仍占位
					对比display:none与visibility:hidden 同样设置元素隐藏,前者不占位,后者依旧在文档中占位
				2.透明度设置
					属性 opacity
					取值 0(完全透明)-1(不透明)
					对比rgba() 与opacity设置透明度,二者的区别:
						rgba()设置的半透明效果不会被子元素继承;
						opacity设置的半透明效果会被子元素继承,如果子元素也设置了透明度,最终子元素的透明度由两个值相乘得到
				3.元素的垂直对齐方式
					属性 vertical-align
					取值 top/middle/bottom/baseline(默认值,基线对齐)
					使用场景
						添加在行内块(img,input)元素上 ,用来设置元素两端的文本与当前元素的对齐方式
			3.光标的外观
				属性 cursor
				取值
					1.default(默认值)
					2.poiner 小手形状
					3.text 显示为 I 样式
8.CSS转换属性
	属性 transform
	取值 转换函数,如果出现多个函数同时使用,要用空格隔开
9.转换原点
	属性 transform-origin
	取值
		1.关键字 top/bottom/left/right/center
		2.使用像素值或是百分比来指定转换原点
		3.默认转化原点在元素的中心位置
10.转换函数
	1.平移转换
		1.转换函数 translate()
		2.取值
			1.translate(x) 元素沿水平方向进行移动,等价于translateX()
			2.translate(x,y) 元素沿水平方向移动x,沿垂直方向移动的y距离
			3.translateY() 元素沿垂直方向移动
	2.缩放
		1.根据比例改变元素大小
		2.语法
			1.转换函数 scale(x) 表示元素x轴和y轴都按照指定比例缩放
			2.scaleX(x) 表示元素x轴按照指定比例缩放
			3.scaleY(x) 表示元素y轴按照指定比例缩放
		3.取值
			取值为无单位的数字,默认值是1(1表示原始尺寸)
			>1 元素放大的比例
			(0-1) 缩小
			<0 元素会反转
	3.旋转
		1.改变元素在文档中的显示角度
		2.属性 rotate(ndeg) n取值deg单位
		3.取值为带角度单位的数值,e.g.:45deg(45°)
			取值为正 表示顺时针旋转
			取值为负 表示逆时针旋转
		4.注意
			1.转换原点一旦改变,会影响转换效果
			2.旋转操作会连带坐标轴一起旋转,会影响其后其他的转换操作
			3. 